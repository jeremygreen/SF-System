<%= form_for(@movement_type) do |f| %>
  <% if @movement_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@movement_type.errors.count, "error") %> prohibited this movement_type from being saved:</h2>

      <ul>
      <% @movement_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :movementtype %>:
    <%= f.text_field :movementtype %>
  </div>
  <div class="actions">
    <%= f.submit :class => 'button' %>&nbsp;<%= submit_tag 'Cancel', :class => 'button' %>
  </div>
<% end %>
